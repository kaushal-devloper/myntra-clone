- [ ] Inspect current SecureStore usage and AuthContext flow
- [ ] Fix SecureStore import/API mismatch causing `getValueWithKeyAsync is not a function`
- [ ] Add safe fallback when SecureStore is unavailable (return null) to prevent app crash
- [ ] Re-test: app launch + category navigation screen in Android APK build
- [ ] If still failing, check expo-secure-store version and run clean build (expo start --clear / rebuild APK)

